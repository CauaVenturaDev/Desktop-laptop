[Unit]
Description=PeriShare - receptor de áudio (reproduz o som remoto nos fones)
After=graphical-session.target sound.target
PartOf=graphical-session.target

[Service]
ExecStart=/usr/bin/perishare audio-recv
Restart=on-failure
RestartSec=3

[Install]
WantedBy=graphical-session.target
